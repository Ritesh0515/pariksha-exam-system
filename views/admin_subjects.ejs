<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <title>Manage Subjects | Pariksha Admin</title>
    <style>
        .glass-card { transition: all 0.3s ease; }
        .subject-code-pill {
            background-color: #eff6ff;
            color: #2563eb;
            font-weight: 700;
            padding: 4px 12px;
            border-radius: 8px;
            border: 1px solid #dbeafe;
            font-family: 'Monaco', 'Consolas', monospace;
        }
        .table thead th { 
            font-size: 0.8rem; 
            text-transform: uppercase; 
            letter-spacing: 1px; 
            color: #64748b;
        }
    </style>
</head>
<body class="bg-light">
<div class="container-fluid">
    <div class="row">
        <%- include('partials/sidebar', {currentPage: 'subjects'}) %>

        <div class="col-md-10 offset-md-2 p-5">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h2 class="fw-bold text-dark mb-0">Subject Management</h2>
                    <p class="text-muted small">Organize your curriculum by adding or editing subjects.</p>
                </div>
                <button class="btn btn-primary shadow-sm px-4 rounded-pill" data-bs-toggle="collapse" data-bs-target="#addSubjectForm">
                    <i class="bi bi-plus-lg me-2"></i>Add Subject
                </button>
            </div>

            <div class="collapse mb-5" id="addSubjectForm">
                <div class="card glass-card p-4 border-0 shadow-sm border-top border-4 border-primary">
                    <h5 class="fw-bold mb-3">Create New Subject</h5>
                    <form action="/admin/subjects/add" method="POST" class="row g-3">
                        <div class="col-md-4">
                            <label class="form-label small fw-bold">Subject Name</label>
                            <input type="text" name="subject_name" class="form-control bg-light border-0 p-2" placeholder="e.g. Data Structures" required>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label small fw-bold">Subject Code</label>
                            <input type="text" name="subject_code" class="form-control bg-light border-0 p-2" placeholder="BCA-201" required>
                        </div>
                        <div class="col-md-3">
                            <label class="form-label small fw-bold">Assign Course</label>
                            <select name="course_id" class="form-select bg-light border-0 p-2">
                                <% courses.forEach(c => { %>
                                    <option value="<%= c.course_id %>"><%= c.course_name %></option>
                                <% }) %>
                            </select>
                        </div>
                        <div class="col-md-2 d-flex align-items-end">
                            <button type="submit" class="btn btn-success w-100 shadow-sm fw-bold p-2">
                                <i class="bi bi-check-lg me-1"></i>Save
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card border-0 shadow-sm overflow-hidden rounded-4">
                <table class="table mb-0 align-middle table-hover bg-white">
                    <thead class="bg-light">
                        <tr>
                            <th class="ps-4 py-3">Subject Code</th>
                            <th>Subject Name</th>
                            <th>Associated Course</th>
                            <th class="text-end pe-4">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (subjects.length > 0) { %>
                            <% subjects.forEach(s => { %>
                            <tr>
                                <td class="ps-4">
                                    <span class="subject-code-pill small"><%= s.subject_code %></span>
                                </td>
                                <td class="fw-semibold text-dark"><%= s.subject_name %></td>
                                <td>
                                    <span class="badge bg-secondary bg-opacity-10 text-secondary border border-secondary-subtle px-3 py-2">
                                        <i class="bi bi-mortarboard me-1"></i><%= s.course_name %>
                                    </span>
                                </td>
                                <td class="text-end pe-4">
                                    <div class="btn-group shadow-sm">
                                        <button class="btn btn-sm btn-white border px-3" onclick="openSubjectModal('<%= s.subject_id %>', '<%= s.subject_name %>', '<%= s.subject_code %>', '<%= s.course_id %>')" title="Edit Subject">
                                            <i class="bi bi-pencil-square text-primary"></i>
                                        </button>
                                        <a href="/admin/subjects/delete/<%= s.subject_id %>" class="btn btn-sm btn-outline-danger px-3" onclick="return confirm('Are you sure you want to delete this subject?')" title="Delete Subject">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                            <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="4" class="text-center py-5">
                                    <i class="bi bi-journals display-4 text-muted opacity-50"></i>
                                    <p class="text-muted mt-2">No subjects found in the database.</p>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="editSubjectModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow-lg">
            <div class="modal-header bg-primary text-white border-0">
                <h5 class="modal-title fw-bold">Update Subject</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <form id="editSubjectForm" method="POST">
                <div class="modal-body p-4">
                    <div class="mb-3">
                        <label class="form-label small fw-bold text-dark">Subject Name</label>
                        <input type="text" name="subject_name" id="edit_sub_name" class="form-control bg-light border-0 p-2" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label small fw-bold text-dark">Subject Code</label>
                        <input type="text" name="subject_code" id="edit_sub_code" class="form-control bg-light border-0 p-2" required>
                    </div>
                    <div class="mb-2">
                        <label class="form-label small fw-bold text-dark">Change Course</label>
                        <select name="course_id" id="edit_course_id" class="form-select bg-light border-0 p-2">
                            <% courses.forEach(c => { %>
                                <option value="<%= c.course_id %>"><%= c.course_name %></option>
                            <% }) %>
                        </select>
                    </div>
                </div>
                <div class="modal-footer border-0 p-4">
                    <button type="button" class="btn btn-light px-4 rounded-pill" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-primary px-4 fw-bold shadow-sm rounded-pill">Save Changes</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
    function openSubjectModal(id, name, code, courseId) {
        document.getElementById('edit_sub_name').value = name;
        document.getElementById('edit_sub_code').value = code;
        document.getElementById('edit_course_id').value = courseId;
        document.getElementById('editSubjectForm').action = '/admin/subjects/update/' + id;
        new bootstrap.Modal(document.getElementById('editSubjectModal')).show();
    }
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>