<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pariksha | Secure Assessment Platform</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Nunito+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --blue-core: #1E3A8A;
      --blue-deep: #172F75;
      --blue-mid: #2a4fa8;
      --blue-glow: #3b6fd4;
      --accent: #60a5fa;
      --accent-bright: #93c5fd;
      --ink: #0a1628;
      --surface: #0f1e3a;
      --surface-2: #162444;
      --surface-3: #1c2e52;
      --border: rgba(96,165,250,0.15);
      --border-strong: rgba(96,165,250,0.3);
      --text-primary: #f0f6ff;
      --text-secondary: #94a9c9;
      --text-muted: #5a7299;
      --success: #34d399;
      --danger: #f87171;
      --warning: #fbbf24;
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: 'Nunito Sans', sans-serif;
      background: var(--ink);
      color: var(--text-primary);
      overflow-x: hidden;
    }

    /* ─── NAV ─── */
    nav {
      position: sticky;
      top: 0;
      z-index: 100;
      background: rgba(10, 22, 40, 0.85);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      border-bottom: 1px solid var(--border);
      padding: 0 2rem;
    }

    .nav-inner {
      max-width: 1160px;
      margin: 0 auto;
      height: 64px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 10px;
      text-decoration: none;
    }

    .brand-icon {
      width: 36px;
      height: 36px;
      background: linear-gradient(135deg, var(--blue-mid), var(--blue-glow));
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1rem;
      color: #fff;
      box-shadow: 0 2px 10px rgba(59,111,212,0.3);
      flex-shrink: 0;
    }

    .brand-name {
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-weight: 700;
      font-size: 1.15rem;
      color: var(--text-primary);
      letter-spacing: 0.02em;
    }

    .brand-name span {
      color: var(--accent);
    }

    .btn-login {
      display: inline-flex;
      align-items: center;
      gap: 7px;
      background: linear-gradient(135deg, var(--blue-mid), var(--blue-glow));
      color: #fff;
      font-family: 'Nunito Sans', sans-serif;
      font-weight: 600;
      font-size: 0.9rem;
      padding: 0.5rem 1.1rem;
      border-radius: 8px;
      text-decoration: none;
      border: 1px solid rgba(255,255,255,0.1);
      transition: all 0.2s;
      box-shadow: 0 2px 10px rgba(42,79,168,0.3);
    }

    .btn-login:hover {
      box-shadow: 0 0 28px rgba(59,111,212,0.55);
      transform: translateY(-1px);
      color: #fff;
    }

    /* ─── HERO ─── */
    .hero {
      position: relative;
      z-index: 1;
      padding: 7rem 2rem 5rem;
      max-width: 1160px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 4rem;
      align-items: center;
    }

    /* Glowing orb behind hero */
    .hero::before {
      content: '';
      position: absolute;
      top: -80px;
      left: -120px;
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(30,58,138,0.2) 0%, transparent 70%);
      pointer-events: none;
      z-index: -1;
    }

    .hero-eyebrow {
      display: inline-flex;
      align-items: center;
      gap: 7px;
      font-size: 0.78rem;
      font-weight: 500;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--accent);
      background: rgba(96,165,250,0.1);
      border: 1px solid var(--border-strong);
      padding: 0.3rem 0.75rem;
      border-radius: 999px;
      margin-bottom: 1.5rem;
    }

    .hero-eyebrow::before {
      content: '';
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: var(--accent);
      box-shadow: 0 0 8px var(--accent);
    }

    .hero-title {
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-size: clamp(2.4rem, 4vw, 3.5rem);
      font-weight: 800;
      line-height: 1.1;
      letter-spacing: -0.02em;
      margin-bottom: 1.4rem;
    }

    .hero-title .line-dim {
      color: var(--text-secondary);
    }

    .hero-title .highlight {
      background: linear-gradient(90deg, var(--accent-bright), var(--blue-glow));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .hero-desc {
      color: var(--text-secondary);
      font-size: 1.02rem;
      line-height: 1.75;
      max-width: 48ch;
      margin-bottom: 2.2rem;
    }

    .hero-cta {
      display: flex;
      gap: 1rem;
      align-items: center;
      flex-wrap: wrap;
    }

    .btn-primary-cta {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: linear-gradient(135deg, var(--blue-mid) 0%, var(--blue-glow) 100%);
      color: #fff;
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-weight: 600;
      font-size: 0.95rem;
      padding: 0.78rem 1.5rem;
      border-radius: 8px;
      text-decoration: none;
      border: 1px solid rgba(255,255,255,0.12);
      transition: all 0.25s;
      box-shadow: 0 4px 16px rgba(42,79,168,0.35), 0 0 0 0 rgba(96,165,250,0);
    }

    .btn-primary-cta:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 32px rgba(42,79,168,0.65), 0 0 0 3px rgba(96,165,250,0.15);
      color: #fff;
    }

    .role-hint {
      font-size: 0.82rem;
      color: var(--text-muted);
    }

    /* stat row */
    .hero-stats {
      display: flex;
      gap: 2rem;
      margin-top: 2.5rem;
      padding-top: 2rem;
      border-top: 1px solid var(--border);
    }

    .stat-item {}

    .stat-num {
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-size: 1.6rem;
      font-weight: 800;
      color: var(--text-primary);
      line-height: 1;
    }

    .stat-num span {
      color: var(--accent);
    }

    .stat-label {
      font-size: 0.78rem;
      color: var(--text-muted);
      margin-top: 3px;
    }

    /* ─── MOCK EXAM CARD ─── */
    .exam-card-wrap {
      position: relative;
    }

    .exam-card-wrap::after {
      content: '';
      position: absolute;
      inset: -20px;
      background: radial-gradient(ellipse at center, rgba(30,58,138,0.15) 0%, transparent 70%);
      pointer-events: none;
      z-index: -1;
    }

    .exam-card {
      background: var(--surface);
      border: 1px solid var(--border-strong);
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 24px 60px rgba(0,0,0,0.5), inset 0 1px 0 rgba(255,255,255,0.06);
      animation: floatCard 5s ease-in-out infinite;
    }

    @keyframes floatCard {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-8px); }
    }

    .card-topbar {
      background: var(--surface-2);
      padding: 0.65rem 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      border-bottom: 1px solid var(--border);
    }

    .dot { width: 10px; height: 10px; border-radius: 50%; }
    .dot-r { background: #ef4444; }
    .dot-y { background: #f59e0b; }
    .dot-g { background: #22c55e; }

    .url-bar {
      margin-left: 6px;
      background: rgba(255,255,255,0.05);
      border: 1px solid var(--border);
      color: var(--text-muted);
      font-size: 0.72rem;
      border-radius: 5px;
      padding: 0.18rem 0.6rem;
      font-family: 'Nunito Sans', sans-serif;
    }

    /* ── mock card internals: light theme to match real exam page ── */
    .exam-card {
      background: #fff !important;
      border-color: #e2e8f0 !important;
      box-shadow: 0 16px 48px rgba(0,0,0,0.35), inset 0 1px 0 rgba(255,255,255,0.8) !important;
    }

    .card-topbar {
      background: #f8fafc !important;
      border-bottom-color: #e2e8f0 !important;
    }

    .url-bar {
      color: #64748b !important;
      background: #fff !important;
      border-color: #e2e8f0 !important;
    }

    .card-body {
      padding: 0;
    }

    /* exam header bar — matches real page top strip */
    .exam-header-bar {
      padding: 0.75rem 1rem;
      border-bottom: 3px solid var(--blue-core);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .exam-name {
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-weight: 700;
      font-size: 0.88rem;
      color: #0f172a;
    }

    .exam-sub {
      font-size: 0.72rem;
      color: #64748b;
      margin-top: 2px;
      display: flex;
      align-items: center;
      gap: 4px;
    }

    .timer {
      display: flex;
      align-items: center;
      gap: 6px;
      border: 2px solid #0f172a;
      color: #0f172a;
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-weight: 700;
      font-size: 0.82rem;
      padding: 0.3rem 0.7rem;
      border-radius: 999px;
      background: #fff;
    }

    /* question block */
    .q-block {
      padding: 0.9rem 1rem 0.75rem;
      border-bottom: 1px solid #f1f5f9;
    }

    .q-top-row {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      gap: 0.75rem;
      margin-bottom: 0;
    }

    .q-left {
      display: flex;
      align-items: flex-start;
      gap: 0.6rem;
      flex: 1;
    }

    .q-num {
      width: 22px;
      height: 22px;
      background: var(--blue-core);
      color: #fff;
      border-radius: 6px;
      font-size: 0.7rem;
      font-weight: 700;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      margin-top: 1px;
      font-family: 'Plus Jakarta Sans', sans-serif;
    }

    .q-text {
      font-size: 0.82rem;
      font-weight: 600;
      color: #0f172a;
      line-height: 1.5;
    }

    .btn-listen {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      font-size: 0.72rem;
      font-weight: 600;
      color: #334155;
      background: #f8fafc;
      border: 1px solid #e2e8f0;
      border-radius: 6px;
      padding: 0.22rem 0.55rem;
      flex-shrink: 0;
      font-family: 'Nunito Sans', sans-serif;
    }

    .options-list {
      padding: 0.5rem 1rem 0.75rem;
    }

    .option {
      border: 1px solid #e2e8f0;
      border-radius: 8px;
      padding: 0.48rem 0.75rem;
      margin-bottom: 0.38rem;
      font-size: 0.79rem;
      color: #334155;
      background: #fff;
      display: flex;
      align-items: center;
      gap: 0.55rem;
      cursor: pointer;
      transition: all 0.15s;
    }

    .option:hover { border-color: #94a3b8; }

    .option.selected {
      background: #eff6ff;
      border-color: var(--blue-core);
      color: var(--blue-core);
      font-weight: 600;
    }

    .opt-key {
      font-weight: 700;
      color: #94a3b8;
      font-size: 0.75rem;
      min-width: 14px;
    }

    .option.selected .opt-key { color: var(--blue-core); }

    /* badge row below card */
    .badge-row {
      display: flex;
      gap: 0.6rem;
      margin-top: 0.85rem;
      flex-wrap: wrap;
    }

    .badge-pill {
      display: inline-flex;
      align-items: center;
      gap: 5px;
      background: var(--surface-2);
      border: 1px solid var(--border);
      color: var(--text-secondary);
      font-size: 0.73rem;
      padding: 0.25rem 0.6rem;
      border-radius: 999px;
    }

    .badge-pill i { color: var(--accent); font-size: 0.8rem; }

    /* ─── FEATURES ─── */
    .features-section {
      position: relative;
      z-index: 1;
      padding: 5rem 2rem;
      background: linear-gradient(180deg, transparent, rgba(15,30,58,0.6) 30%, rgba(15,30,58,0.6) 70%, transparent);
    }

    .section-label {
      text-align: center;
      font-size: 0.75rem;
      font-weight: 500;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--accent);
      margin-bottom: 0.75rem;
    }

    .section-title {
      text-align: center;
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-size: clamp(1.6rem, 3vw, 2.2rem);
      font-weight: 800;
      color: var(--text-primary);
      margin-bottom: 0.6rem;
      line-height: 1.2;
    }

    .section-sub {
      text-align: center;
      color: var(--text-secondary);
      font-size: 0.95rem;
      max-width: 55ch;
      margin: 0 auto 3.5rem;
      line-height: 1.7;
    }

    .features-grid {
      max-width: 1160px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.25rem;
    }

    .feat-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 1.5rem;
      transition: all 0.25s;
      position: relative;
      overflow: hidden;
    }

    .feat-card::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--accent), transparent);
      opacity: 0;
      transition: opacity 0.3s;
    }

    .feat-card:hover {
      border-color: var(--border-strong);
      transform: translateY(-3px);
      box-shadow: 0 16px 40px rgba(0,0,0,0.35);
    }

    .feat-card:hover::before { opacity: 1; }

    .feat-icon {
      width: 42px;
      height: 42px;
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.1rem;
      margin-bottom: 1.1rem;
    }

    .feat-icon.blue { background: rgba(30,58,138,0.5); color: var(--accent); border: 1px solid var(--border-strong); }
    .feat-icon.teal { background: rgba(52,211,153,0.1); color: var(--success); border: 1px solid rgba(52,211,153,0.2); }
    .feat-icon.amber { background: rgba(251,191,36,0.1); color: var(--warning); border: 1px solid rgba(251,191,36,0.2); }
    .feat-icon.red { background: rgba(248,113,113,0.1); color: var(--danger); border: 1px solid rgba(248,113,113,0.2); }
    .feat-icon.indigo { background: rgba(129,140,248,0.1); color: #a5b4fc; border: 1px solid rgba(129,140,248,0.2); }
    .feat-icon.sky { background: rgba(56,189,248,0.1); color: #7dd3fc; border: 1px solid rgba(56,189,248,0.2); }

    .feat-title {
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-weight: 700;
      font-size: 1rem;
      color: var(--text-primary);
      margin-bottom: 0.5rem;
    }

    .feat-desc {
      font-size: 0.87rem;
      color: var(--text-secondary);
      line-height: 1.65;
    }

    /* ─── ROLES ─── */
    .roles-section {
      position: relative;
      z-index: 1;
      padding: 5rem 2rem;
      max-width: 1160px;
      margin: 0 auto;
    }

    .roles-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1.25rem;
      margin-top: 3.5rem;
    }

    .role-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 1.75rem 1.5rem;
      text-align: center;
      transition: all 0.25s;
    }

    .role-card:hover {
      border-color: var(--border-strong);
      transform: translateY(-3px);
      box-shadow: 0 16px 40px rgba(0,0,0,0.3);
    }

    .role-avatar {
      width: 60px;
      height: 60px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      margin: 0 auto 1rem;
    }

    .role-avatar.admin { background: rgba(30,58,138,0.4); color: var(--accent); border: 1px solid var(--border-strong); }
    .role-avatar.faculty { background: rgba(52,211,153,0.1); color: var(--success); border: 1px solid rgba(52,211,153,0.2); }
    .role-avatar.student { background: rgba(251,191,36,0.1); color: var(--warning); border: 1px solid rgba(251,191,36,0.2); }

    .role-title {
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-weight: 700;
      font-size: 1.05rem;
      color: var(--text-primary);
      margin-bottom: 0.5rem;
    }

    .role-desc {
      font-size: 0.85rem;
      color: var(--text-secondary);
      line-height: 1.65;
      margin-bottom: 1.2rem;
    }

    .role-tag-list {
      display: flex;
      flex-wrap: wrap;
      gap: 0.4rem;
      justify-content: center;
    }

    .role-tag {
      font-size: 0.72rem;
      background: rgba(255,255,255,0.05);
      border: 1px solid var(--border);
      color: var(--text-muted);
      padding: 0.2rem 0.55rem;
      border-radius: 999px;
    }

    /* ─── CTA BAND ─── */
    .cta-band {
      position: relative;
      z-index: 1;
      margin: 2rem 2rem 5rem;
      max-width: 1160px;
      margin-left: auto;
      margin-right: auto;
    }

    .cta-inner {
      background: linear-gradient(135deg, var(--blue-deep) 0%, var(--blue-mid) 50%, var(--blue-glow) 100%);
      border-radius: 20px;
      padding: 3.5rem 3rem;
      text-align: center;
      position: relative;
      overflow: hidden;
      border: 1px solid rgba(255,255,255,0.1);
      box-shadow: 0 24px 60px rgba(30,58,138,0.4);
    }

    .cta-inner::before {
      content: '';
      position: absolute;
      top: -60px; right: -60px;
      width: 250px; height: 250px;
      background: radial-gradient(circle, rgba(255,255,255,0.08) 0%, transparent 70%);
      pointer-events: none;
    }

    .cta-inner::after {
      content: '';
      position: absolute;
      bottom: -40px; left: -40px;
      width: 200px; height: 200px;
      background: radial-gradient(circle, rgba(255,255,255,0.05) 0%, transparent 70%);
      pointer-events: none;
    }

    .cta-title {
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-size: clamp(1.6rem, 3vw, 2.1rem);
      font-weight: 800;
      color: #fff;
      margin-bottom: 0.75rem;
    }

    .cta-sub {
      color: rgba(255,255,255,0.75);
      font-size: 0.97rem;
      margin-bottom: 2rem;
      max-width: 50ch;
      margin-left: auto;
      margin-right: auto;
      line-height: 1.7;
    }

    .btn-cta-white {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: #fff;
      color: var(--blue-deep);
      font-family: 'Plus Jakarta Sans', sans-serif;
      font-weight: 700;
      font-size: 0.97rem;
      padding: 0.8rem 1.8rem;
      border-radius: 9px;
      text-decoration: none;
      transition: all 0.25s;
      box-shadow: 0 4px 20px rgba(0,0,0,0.2);
    }

    .btn-cta-white:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 30px rgba(0,0,0,0.3);
      color: var(--blue-deep);
    }

    /* ─── FOOTER ─── */
    footer {
      position: relative;
      z-index: 1;
      border-top: 1px solid var(--border);
      padding: 1.4rem 2rem;
      background: rgba(10,22,40,0.8);
    }

    .footer-inner {
      max-width: 1160px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 0.75rem;
    }

    .footer-left {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .footer-copy {
      font-size: 0.84rem;
      color: var(--text-muted);
    }

    .footer-link {
      font-size: 0.84rem;
      color: var(--accent);
      text-decoration: none;
      font-weight: 500;
    }

    .footer-link:hover {
      color: var(--accent-bright);
    }

    /* ─── ANIMATIONS ─── */
    .fade-up {
      opacity: 0;
      transform: translateY(24px);
      transition: opacity 0.6s ease, transform 0.6s ease;
    }

    .fade-up.visible {
      opacity: 1;
      transform: translateY(0);
    }

    /* ─── RESPONSIVE ─── */
    @media (max-width: 900px) {
      .hero {
        grid-template-columns: 1fr;
        padding: 4rem 1.5rem 3rem;
        gap: 2.5rem;
      }

      .features-grid,
      .roles-grid {
        grid-template-columns: 1fr;
        max-width: 520px;
        margin-left: auto;
        margin-right: auto;
      }

      .hero-stats { gap: 1.5rem; }
    }

    @media (max-width: 600px) {
      .features-grid,
      .roles-grid {
        grid-template-columns: 1fr;
      }

      .cta-inner { padding: 2.5rem 1.5rem; }
    }
  </style>
</head>
<body>

  <!-- NAV -->
  <nav>
    <div class="nav-inner">
      <a href="/" class="brand">
        <span class="brand-icon"><i class="bi bi-shield-lock-fill"></i></span>
        <span class="brand-name">Parik<span>sha</span></span>
      </a>
      <a href="/login" class="btn-login">
        <i class="bi bi-box-arrow-in-right"></i> Login
      </a>
    </div>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-left">
      <div class="hero-eyebrow">Secure Assessment Platform</div>
      <h1 class="hero-title">
        Exams that<br>
        <span class="line-dim">can't be</span><br>
        <span class="highlight">compromised.</span>
      </h1>
      <p class="hero-desc">
        Pariksha is an end-to-end examination system built for institutions — with live proctoring, 
        role-based access, and analytics that give faculty real clarity on outcomes.
      </p>
      <div class="hero-cta">
        <a href="/login" class="btn-primary-cta">
          Access Portal <i class="bi bi-arrow-right"></i>
        </a>
        <span class="role-hint">Students · Faculty · Admins</span>
      </div>
      <div class="hero-stats">
        <div class="stat-item">
          <div class="stat-num">3<span>+</span></div>
          <div class="stat-label">User Roles</div>
        </div>
        <div class="stat-item">
          <div class="stat-num">A<span>/</span>B</div>
          <div class="stat-label">Set Randomisation</div>
        </div>
        <div class="stat-item">
          <div class="stat-num">0<span>%</span></div>
          <div class="stat-label">Tolerance for cheating</div>
        </div>
      </div>
    </div>

    <div class="exam-card-wrap">
      <div class="exam-card">
        <div class="card-topbar">
          <span class="dot dot-r"></span>
          <span class="dot dot-y"></span>
          <span class="dot dot-g"></span>
          <span class="url-bar">secure.pariksha.edu/exam/attempt</span>
        </div>
        <div class="exam-header-bar">
          <div>
            <div class="exam-name">Python — Mid-semester 2025</div>
            <div class="exam-sub"><i class="bi bi-person-circle"></i> Example Student &nbsp;·&nbsp; Set A</div>
          </div>
          <span class="timer"><i class="bi bi-clock"></i> 19:57</span>
        </div>

        <!-- Q1 -->
        <div class="q-block">
          <div class="q-top-row">
            <div class="q-left">
              <span class="q-num">1</span>
              <span class="q-text">What type of value is created by this line of Python code: name = "Alex"?</span>
            </div>
            <span class="btn-listen"><i class="bi bi-volume-up-fill"></i> Listen</span>
          </div>
          <div class="options-list">
            <div class="option"><span class="opt-key">A:</span> A float, because all variables are floats by default</div>
            <div class="option"><span class="opt-key">B:</span> A boolean, because it will be either true or false</div>
            <div class="option"><span class="opt-key">C:</span> An integer, because it has no decimal point</div>
            <div class="option selected"><span class="opt-key">D:</span> A string, because it is text inside quotes</div>
          </div>
        </div>

        <!-- Q2 partial -->
        <div class="q-block" style="border-bottom:none;">
          <div class="q-top-row">
            <div class="q-left">
              <span class="q-num">2</span>
              <span class="q-text">Which of the following is the best way to store the whole number 7 in a variable and then print it in Python?</span>
            </div>
            <span class="btn-listen"><i class="bi bi-volume-up-fill"></i> Listen</span>
          </div>
          <div class="options-list">
            <div class="option"><span class="opt-key">A:</span> x = 7 print(x)</div>
            <div class="option"><span class="opt-key">B:</span> x = "7" print(x)</div>
          </div>
        </div>
      </div>

      <div class="badge-row">
        <span class="badge-pill"><i class="bi bi-shield-check"></i> Tab-switch monitoring active</span>
        <span class="badge-pill"><i class="bi bi-volume-up"></i> TTS available</span>
        <span class="badge-pill"><i class="bi bi-lock-fill"></i> Browser locked</span>
      </div>
    </div>
  </section>

  <!-- FEATURES -->
  <section class="features-section">
    <div class="section-label">Platform Capabilities</div>
    <h2 class="section-title">Everything an institution needs.</h2>
    <p class="section-sub">
      From question bank creation to post-exam analytics — built to handle real academic workflows end-to-end.
    </p>

    <div class="features-grid">
      <div class="feat-card fade-up">
        <div class="feat-icon blue"><i class="bi bi-shield-check"></i></div>
        <div class="feat-title">Live Proctoring</div>
        <p class="feat-desc">Real-time tab-switch detection and a browser lockdown environment give administrators a clean, verifiable event trail for every student session.</p>
      </div>
      <div class="feat-card fade-up" style="transition-delay:0.07s">
        <div class="feat-icon teal"><i class="bi bi-shuffle"></i></div>
        <div class="feat-title">Set A / B Randomisation</div>
        <p class="feat-desc">Adjacent students automatically receive different question sets, eliminating the simplest form of academic dishonesty without any extra setup.</p>
      </div>
      <div class="feat-card fade-up" style="transition-delay:0.14s">
        <div class="feat-icon amber"><i class="bi bi-bar-chart-line"></i></div>
        <div class="feat-title">Faculty Analytics</div>
        <p class="feat-desc">Drill into participation rates, pass/fail ratios, average scores, and Set Mix distributions. Make data-driven decisions about your curriculum.</p>
      </div>
      <div class="feat-card fade-up" style="transition-delay:0.21s">
        <div class="feat-icon sky"><i class="bi bi-volume-up-fill"></i></div>
        <div class="feat-title">Text-to-Speech (TTS)</div>
        <p class="feat-desc">Students can listen to any question aloud, supporting accessibility for those with reading difficulties or visual impairments — built in, not bolted on.</p>
      </div>
      <div class="feat-card fade-up" style="transition-delay:0.28s">
        <div class="feat-icon indigo"><i class="bi bi-upload"></i></div>
        <div class="feat-title">Bulk CSV Import</div>
        <p class="feat-desc">Import entire question banks via CSV with automatic duplicate detection and cleanup. Save hours of manual data entry for every exam cycle.</p>
      </div>
    </div>
  </section>

  <!-- ROLES -->
  <section class="roles-section">
    <div class="section-label">Who It's For</div>
    <h2 class="section-title">One platform, three roles.</h2>
    <p class="section-sub">
      A single sign-on grants role-appropriate access — no confusion, no overlap.
    </p>

    <div class="roles-grid">
      <div class="role-card fade-up">
        <div class="role-avatar admin"><i class="bi bi-gear-fill"></i></div>
        <div class="role-title">Administrators</div>
        <p class="role-desc">Manage the full infrastructure — user registration, course creation, system-wide settings, and bulk operations.</p>
        <div class="role-tag-list">
          <span class="role-tag">User management</span>
          <span class="role-tag">Course setup</span>
          <span class="role-tag">CSV import</span>
        </div>
      </div>
      <div class="role-card fade-up" style="transition-delay:0.1s">
        <div class="role-avatar faculty"><i class="bi bi-person-workspace"></i></div>
        <div class="role-title">Faculty / Staff</div>
        <p class="role-desc">Build question banks, design exams with Set A/B logic, monitor live sessions, and analyse post-exam results.</p>
        <div class="role-tag-list">
          <span class="role-tag">Question banks</span>
          <span class="role-tag">Live monitoring</span>
          <span class="role-tag">Analytics</span>
          <span class="role-tag">Marksheets</span>
        </div>
      </div>
      <div class="role-card fade-up" style="transition-delay:0.2s">
        <div class="role-avatar student"><i class="bi bi-mortarboard-fill"></i></div>
        <div class="role-title">Students</div>
        <p class="role-desc">View upcoming exams, attempt assessments in the secure locked interface, and review performance history.</p>
        <div class="role-tag-list">
          <span class="role-tag">Upcoming exams</span>
          <span class="role-tag">Secure attempt</span>
          <span class="role-tag">TTS support</span>
          <span class="role-tag">Results history</span>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA BAND -->
  <div class="cta-band">
    <div class="cta-inner">
      <h2 class="cta-title">Ready to take your exam?</h2>
      <p class="cta-sub">Log in with your institutional credentials. Your role and exams are waiting for you inside.</p>
      <a href="/login" class="btn-cta-white">
        <i class="bi bi-shield-lock-fill"></i> Go to Login Portal
      </a>
    </div>
  </div>

  <!-- FOOTER -->
  <footer>
    <div class="footer-inner">
      <div class="footer-left">
        <span class="brand-icon" style="width:28px;height:28px;font-size:0.82rem;"><i class="bi bi-shield-lock-fill"></i></span>
        <span class="footer-copy">Pariksha &copy; 2026 &mdash; Institutional Assessment Infrastructure</span>
      </div>
      <a href="/login" class="footer-link">Unified Login Portal →</a>
    </div>
  </footer>

  <script>
    // Scroll-triggered fade-up
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(e => {
        if (e.isIntersecting) {
          e.target.classList.add('visible');
          observer.unobserve(e.target);
        }
      });
    }, { threshold: 0.12 });

    document.querySelectorAll('.fade-up').forEach(el => observer.observe(el));

    // Option click interaction on mock card
    document.querySelectorAll('.option').forEach(opt => {
      opt.addEventListener('click', () => {
        document.querySelectorAll('.option').forEach(o => o.classList.remove('selected'));
        opt.classList.add('selected');
      });
    });
  </script>
</body>
</html> 