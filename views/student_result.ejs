<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <title>Exam Result | Pariksha</title>
</head>
<body class="bg-light">
    <div class="container py-5 text-center">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card glass-card p-5 border-0 shadow-sm">
                    <h2 class="fw-bold mb-4">Exam Results</h2>
                    <div class="display-4 fw-bold mb-2 <%= status === 'PASSED' ? 'text-success' : 'text-danger' %>">
                        <%= score %> / <%= total %>
                    </div>
                    <h3 class="fw-bold mb-4"><%= status %></h3>
                    
                    <p class="text-muted">Exam: <strong><%= exam.exam_name %></strong></p>
                    
                    <div class="d-grid mt-4">
                        <a href="/student/dashboard" class="btn btn-primary">Return to Dashboard</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>